apiVersion: v1
kind: Service
metadata:
  name: twins-backend-canary
  namespace: openclaw-twins
  labels:
    app: twins-backend
    canary: "true"
spec:
  selector:
    app: twins-backend
    canary: "true"
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
